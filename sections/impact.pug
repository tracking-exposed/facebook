doctype html
html
    head
        link(rel='stylesheet', href='/css/c3.css')
        link(rel='stylesheet', href='/css/bootstrap.css')
        link(rel='stylesheet', href='/css/index.css')

        script(type='text/javascript', src='/js/lodash.min.js')
        script(type='text/javascript', src='/js/jquery.js')
        script(type='text/javascript', src='/js/bootstrap.min.js')
        script(type='text/javascript', src='/js/d3.min.js')
        script(type='text/javascript', src='/js/c3.min.js')
        script(type='text/javascript', src='/js/moment.min.js')
        script(type='text/javascript', src='/js/local/stats.js')

        meta(charset="utf-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        meta(name="keywords" content="facebook tracking exposed")
        meta(property="og:title" content="facebook.tracking.exposed")
        meta(property="og:type" content="website")
        meta(property="og:description" content="facebook.tracking.exposed Open Statistics. why keep them secret? If we share our stats we might reduce the monopoly of google-analytics")
        meta(property="og:url" content="https://facebook.tracking.exposed")
        meta(property="og:image" content="https://facebook.tracking.exposed/images/fbtrex_logo-400x400.png")
        meta(property="og:image:width" content="400")
        meta(property="og:image:height" content="400")
        title Adoption trends, metadata extraction and open statistics

    body

        .simpletext
            include navbar

        .container-fluid
            .simpletext
                h2 Adoption statistics
                p Since the mid of December 2016, facebook.tracking.exposed is in ß release. below some graph of the data collected. As part of the stable delivery, we'll define how third party can download the dataset and how we might integrate third party analysis in our APIs.
                p We know to be in a slippery terrain: in one side, enabling users to assess Personalisation Algorithm is our goal, in the other, we value Privacy and Identity rights much more than the corporations we criticize. 
                    a(href="https://responsibledata.io/social-media-intelligence-the-wayward-child-of-open-source-intelligence/") Aware of the risks posed by social media intelligence
                    |, we aim to enable users awareness, but prevent abuses.

            .simpletext
                p Raw numbers
                table
                    thead
                        tr 
                            th.col-sm-1 supporters
                            th.col-sm-1 timelines
                            th.col-sm-1 impressions
                            th.col-sm-1 htmls
                            th.col-sm-1 accesses
                    tbody
                        tr 
                            th.col-sm-1 #{supporters}
                            th.col-sm-1 #{timelines}
                            th.col-sm-1 #{impressions}
                            th.col-sm-1 #{htmls}
                            th.col-sm-1 #{accesses}

        .container-fluid
            .simpletext
                h2 since 2019 start, this tool is in renovation: no stats available at the moment.
                smaller --- 12 January 2019
        //-
            .container-fluid
                .simpletext
                    h2 Supporters, Page views
                div(id="users")

                .simpletext
                    h2 Views by Country
                div(id="viewscountries")

                .simpletext
                    h2 Timelines by Country
                div(id="contribcountries")

                .simpletext
                    h2 Impressions collected
                div(id="goldmine")

                .simpletext
                    h2 Metadata available
                    p The analysis is made by the parser, 
                        a(href="https://github.com/tracking-exposed/facebook/issues/44") try to see how they works
                div(id="metadata")

                .simpletext
                    h2 How much an user keep using the extension?
                div(id="engagement")
        //-

        div.footer
