const _ = require('lodash');
const debug = require('debug')('parsers:components:textsearch');

function textsearch(envelop, previous) {

// TODO find a way to let people search for the text they might want
// the function is async because should look at DB/JSON to find out if supporter
// has anything configured
throw new Error("xxx");

/*
    const matc= envelop.html.html.match(//);

    if(matc) debugger;
    if(matc) return { matc };
*/
};

module.exports = textsearch;
